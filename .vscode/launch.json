{
    "configurations": [
        
        {
            "name": "🐍 Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "envFile": "${workspaceFolder}/.env"
            }
        },
        {
            "name": "🛠️Spider-Android(FastAPI)",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app:app",
                "--reload",
                "--host",
                "0.0.0.0",
                "--port",
                "7002"
            ],
            "cwd": "${workspaceFolder}"
        }
    ],
    "inputs": [
    ]
}